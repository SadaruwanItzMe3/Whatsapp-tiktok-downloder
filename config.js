// config.js

module.exports = {
    // TikTok API endpoint
    tikTokApi: 'https://api.tikwm.com', // Free TikTok API
    tikTokApiKey: '', // Optional, if the API needs a key (for now empty)

    // Command Prefix
    prefix: '.'
};
